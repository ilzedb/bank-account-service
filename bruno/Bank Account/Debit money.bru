meta {
  name: Debit money
  type: http
  seq: 5
}

post {
  url: http://localhost:8080/api/v1/accounts/ID1/debit
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "currency": "SEK",
    "amount": 1
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
