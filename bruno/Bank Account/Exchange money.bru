meta {
  name: Exchange money
  type: http
  seq: 6
}

post {
  url: http://localhost:8080/api/v1/accounts/ID1/exchange
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "fromCurrency": "USD",
    "toCurrency": "GBP",
    "amount": 0.01
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
